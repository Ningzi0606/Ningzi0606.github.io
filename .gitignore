# 系统与编辑器
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# 依赖与日志
node_modules/
npm-debug.log*
*.log

# ── 自动生成的网页（由 build/build.js 产出，不提交）────────────────
# 源文件（content/ 下的 .md、图片、音乐，以及 assets/、build/）才是要跟踪的。
# 这样每次改一个字的文章，不会产生一整套新的 HTML 版本，仓库不会随时间膨胀。
# GitHub Actions 在部署时会自动重新生成，所以线上完全不受影响。
/index.html
/404.html
/posts/
/photos/
/videos/
/about/
/music/

# 转码用的临时工具（ffmpeg 等，约 185MB，不要提交）
.tools/

# 已被取代的旧素材（保留在本地备查，不提交）
_unused/

# 视频源分片（音视频分离的原始分片，合并成 mp4 后就没用了）
content/media/*.m4s

# 无损音频体积很大，不建议提交（需要就取消注释）
# content/music/*.wav
# content/music/*.flac

# 大文件（如果你不想把它们提交到仓库，取消下面的注释）
# content/media/*.mp4
# assets/video/*.mp4
